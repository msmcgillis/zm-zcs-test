version: '3.4'

services:
  perf:
    image: ${IMG}
    container_name: perf
    hostname: localhost
    build:
      context: '.'
      dockerfile: 'build/Dockerfile'
    volumes:
      - ./logs:/opt/qa/logs
    entrypoint:
      - /zimbra/zm-test
      - --service
    network_mode: "host"
